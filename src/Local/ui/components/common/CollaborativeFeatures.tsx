import React from 'react';

/**
 * 共同編集用機能パネル（Local版 - 無効化）
 * ローカルモードでは共同編集機能は利用できません
 */
const CollaborativeFeatures = () => {
  // ローカルモードでは常に非表示
  return null;
};

export default CollaborativeFeatures;