/**
 * シンプル版のメインエントリーポイント（Local専用）
 */

import React from 'react';
import MindMapApp from './Local/ui/components/mindmap/MindMapApp';

const SimpleApp: React.FC = () => {
  return <MindMapApp />;
};

export default SimpleApp;