/**
 * シンプル版のメインエントリーポイント
 */

import React from 'react';
import { SimpleMindMapApp } from './ui/components/common/SimpleMindMapApp';

const SimpleApp: React.FC = () => {
  return <SimpleMindMapApp />;
};

export default SimpleApp;